services:
  ojp-nova:
    build:
      context: .
      platforms:
        - linux/amd64
        - linux/arm64
    image: skiplus-ojp-nova
    volumes:
      - /var/skiplus/logs:/app/logs
      - /var/skiplus/generated:/app/generated
    ports:
      - "81:80"

